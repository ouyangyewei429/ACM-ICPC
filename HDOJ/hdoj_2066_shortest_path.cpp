/*
PROG:   一个人的旅行
ID  :   ouyangyewei
LANG:   C++
*/
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <memory.h>

const int maxn = 1004;

int T, S, D, M, ss[maxn], tt[maxn], dist[maxn];
struct EDGE
{
    int u, v, w;
}edge[maxn*maxn];

int main()
{
    int i, k;
    while (~scanf("%d %d %d", &T, &S, &D))
    {
        //for ()
    }
    
    return 0;
}
